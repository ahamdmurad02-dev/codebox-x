<Window x:Class="CodeBoxX.Views.AiAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CodeBox X - AI Assistant" Width="520" Height="740" MinWidth="410" MinHeight="520" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextBrush}" Closing="AiAssistantWindow_Closing">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="60" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"><Grid Margin="15,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel VerticalAlignment="Center"><TextBlock Text="AI Assistant" FontSize="18" FontWeight="SemiBold" /><TextBlock Text="Gemini 3.1 Flash-Lite" Foreground="{DynamicResource AccentBrush}" FontSize="11" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><Button Content="AI Settings" Click="Settings_Click" Margin="0,1,6,1" /><Button Content="Clear Chat" Click="ClearChat_Click" Margin="0,1" /></StackPanel></Grid></Border>
        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="9"><WrapPanel><Button Content="Explain" Tag="Explain" Click="Action_Click" Margin="1" /><Button Content="Fix" Tag="Fix" Click="Action_Click" Margin="1" /><Button Content="Refactor" Tag="Refactor" Click="Action_Click" Margin="1" /><Button Content="Generate" Tag="Generate" Click="Action_Click" Margin="1" /><Button Content="Add Comments" Tag="AddComments" Click="Action_Click" Margin="1" /><Button Content="Ask Project" Tag="ProjectQuestion" Click="Action_Click" Margin="1" /></WrapPanel></Border>
        <ListBox x:Name="ChatList" Grid.Row="2" Margin="8" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto"><ListBox.ItemTemplate><DataTemplate><Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="2,4"><StackPanel><TextBlock Text="{Binding Header}" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" /><TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,6,0,0" FontFamily="Cascadia Mono, Segoe UI" /></StackPanel></Border></DataTemplate></ListBox.ItemTemplate></ListBox>
        <Border Grid.Row="3" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="10"><TextBox x:Name="PromptBox" MinHeight="68" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" ToolTip="Ask Gemini about the current code or project" /></Border>
        <Border Grid.Row="4" Background="{DynamicResource PanelBackgroundBrush}" Padding="10,6"><DockPanel><TextBlock x:Name="StatusText" Text="Ready" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" DockPanel.Dock="Left" TextTrimming="CharacterEllipsis" /><StackPanel Orientation="Horizontal" DockPanel.Dock="Right"><Button x:Name="CopyButton" Content="Copy Response" Click="Copy_Click" Margin="0,1,6,1" /><Button x:Name="InsertButton" Content="Insert Code" Click="Insert_Click" Margin="0,1,6,1" /><Button x:Name="SendButton" Content="Send" Style="{StaticResource AccentButton}" Click="Send_Click" Margin="0,1" /></StackPanel></DockPanel></Border>
    </Grid>
</Window>
